noop () { @as "noop.txt"
  set -x

  echo "NOOP"
}

# Install bake
install () {
  # TODO
  echo "Run command \`export PATH=${__BAKE_BINS__}:\${PATH}\`"
}

# Run bash linting
lint () { @from "noop.txt"
  local args=("$@")
  local sources=(./bin/bake ./lib/**)

  set -x

  shellcheck ${args+"${args[@]}"} "${sources[@]}"
  shellcheck --shell bash ${args+"${args[@]}"} ./Bakefile
}

tests () {
  # TODO: run test scenarios
  true
}

# Show this help
help () { @default
  bake:utils:list
}
