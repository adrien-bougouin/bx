GLOBAL_STRING=global-string
GLOBAL_ARRAY=(global-arr-1 global-arr-2 global-arr-3)

show-globals() {
  echo "- GLOBAL_STRING=${GLOBAL_STRING}"
  echo "- GLOBAL_ARRAY=${GLOBAL_ARRAY[*]}"
}

show-globals--subprocess() (
  show-globals
)
